{
  "name": "website_final",
  "version": "1.0.0",
  "main": "products.js",
  "scripts": {
    "build": "node --check netlify/functions/submit-lead.js && node --check netlify/functions/dropship-agent.js && node --check tools/catalog-updater.js",
    "test": "npm run build",
    "catalog:update": "node tools/catalog-updater.js",
    "deploy": "netlify deploy --prod --dir=. --functions=netlify/functions"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "body-parser": "^2.2.2",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "stripe": "^22.1.1"
  },
  "devDependencies": {
    "netlify-cli": "^26.0.2"
  }
}
